//#!/bin/C++
//incllude tells the compiler to include the standard iostream file which contains declarations of all the standard input/output library functions
#include<iostream>

using namespace std;

//Execute the program
int main() {
    cout << "Hello World!"; 
    cout << "I'm a C++ Program.";
    return 0; //this terminates the main() function and return 0
}
